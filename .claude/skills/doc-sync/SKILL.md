---
name: doc-sync
description: コードの変更とドキュメント（PROJECT.md / orbit-roadmap.md / ADR / advisor / rules）のズレを検出して同期する。「ドキュメント同期して」「ドキュメントを最新化」「docs更新」などで使う。マージ後・機能完成時に実行する。
argument-hint: "[PR番号 | コミット範囲]（省略時: main との差分 + 直近のマージ5件）"
---

# ドキュメント同期

コードの実態とドキュメントのズレを検出し、修正する。Claude / Codex 共用スキル。

## 対象範囲の特定

- PR 番号指定: `gh pr view <N>` + `gh pr diff <N>` でその PR の変更を対象にする
- コミット範囲指定: `git diff <範囲> --stat` で対象を把握する
- 省略時: カレントブランチと `main` の差分に加えて、直近のマージ済み PR 5件
  （`gh pr list --state merged --limit 5`）のタイトル・差分概要を対象にする

## 同期チェックマップ（この表の左が変わったら右を確認する）

| コード側の変更 | 確認・更新するドキュメント |
|---|---|
| 機能の追加・変更・削除 | `docs/ai/PROJECT.md` §5/§6 の機能一覧・運用メモ |
| テーブル・migration の追加/変更 | `docs/ai/PROJECT.md` の「主要DBテーブル」、RLS 方針 |
| ADR 追加 | `docs/ai/PROJECT.md` の ADR 表 |
| 技術的負債の解消・発生 | `docs/orbit-roadmap.md` の「技術的負債・既知の制限」表（解決済みは既存様式どおり `~~取り消し線~~` + `✅ Issue #N で対応済み`） |
| フェーズ/計画の進捗 | `docs/orbit-roadmap.md` の該当フェーズ |
| 監査指摘への対応（Issue クローズ） | `docs/advisor/` の該当ドキュメントの Issue 対応表 |
| ルール・運用の変更 | `rules/` ↔ `CLAUDE.md` / `AGENTS.md` の整合（**マスタは rules/**。役割別ファイルには複製せず参照させる） |
| 定型手順の変更 | `.claude/skills/` / `.agents/skills/` の該当スキルの手順 |

## 追加チェック（差分に関わらず毎回）

- Issue に「Decision を残す」とされた変更（新規ライブラリ・層構造・データモデル・重要 UX）が
  `rules/process.md` の基準で ADR 昇格に該当しないか
- ドキュメント内の相互参照（Issue 番号・ファイルパス・ADR 番号）が実在するか

## 修正の実施

- ズレを一覧化してから修正する（何を根拠にズレと判断したかを添える）
- 事実の更新（済/未済、番号、一覧への追加）はそのまま修正する
- **意味・方針を変える修正**（運用メモの書き換え、ルールの変更など）は、修正案を提示して
  ユーザーの確認を取ってから行う
- 修正はドキュメントのみ。コード側を「ドキュメントに合わせて」変更しない
  （コードが正でドキュメントが古い、が原則。逆と思われる場合は報告して確認する）

## 完了条件

- 検出したズレがすべて「修正済み」か「要確認として報告済み」になっている
- 修正内容のサマリー（ファイルごとの変更理由）を提示している
